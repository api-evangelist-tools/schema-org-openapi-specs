---
$id: http://schema.org.apievangelist.com/schema/objects/CancelAction.json
schema: http://json-schema.org/draft-07/schema#
description: 'The act of asserting that a future event/action is no longer going to
  happen.  Related actions:   ConfirmAction: The antonym of CancelAction.  '
properties:
  scheduledTime:
    description: The time the object is scheduled to.
    type: string
    format: date-time
...
