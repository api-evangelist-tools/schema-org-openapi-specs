---
$id: http://schema.org.apievangelist.com/schema/objects/DayOfWeek.json
schema: http://json-schema.org/draft-07/schema#
description: The day of the week, e.g. used to specify to which day the opening hours
  of an OpeningHoursSpecification refer.  Originally, URLs from GoodRelations were
  used (for Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday plus a
  special entry for PublicHolidays); these have now been integrated directly into
  schema.org.
properties: []
...
