---
$id: http://schema.org.apievangelist.com/schema/objects/HowToTool.json
schema: http://json-schema.org/draft-07/schema#
description: A tool used (but not consumed) when performing instructions for how to
  achieve a result.
properties:
  requiredQuantity:
    description: The required quantity of the item(s).
    type: number
    format: float
...
